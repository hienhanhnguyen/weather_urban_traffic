import { PlaceholderPage } from "@/features/shell/PlaceholderPage";

export default function MapPage() {
  return (
    <PlaceholderPage
      title="Map"
      description="An interactive map of conditions across the city, with a location picker."
    />
  );
}
