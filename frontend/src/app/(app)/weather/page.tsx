import { PlaceholderPage } from "@/features/shell/PlaceholderPage";

export default function WeatherPage() {
  return (
    <PlaceholderPage
      title="Weather"
      description="Current conditions and the forecast for each of your saved locations."
    />
  );
}
