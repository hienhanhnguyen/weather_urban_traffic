import { PlaceholderPage } from "@/features/shell/PlaceholderPage";

export default function GovIncidentsPage() {
  return (
    <PlaceholderPage
      title="Incidents"
      description="Reported incidents, their status and who is handling them."
    />
  );
}
